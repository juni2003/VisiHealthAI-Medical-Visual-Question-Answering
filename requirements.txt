# VisiHealth AI - Package Requirements (Updated for Kaggle compatibility)
# Compatible versions that work with Kaggle's existing packages

# Core - Use newer numpy to avoid conflicts
numpy>=1.26.4,<2.0

# Image processing
Pillow==9.5.0
opencv-python-headless==4.9.0.80

# Data augmentation  
albumentations>=1.3.1

# NLP and transformers - Use newer versions compatible with Kaggle
huggingface-hub>=0.25.0
transformers>=4.41.0
tokenizers>=0.15.1

# Note: PyTorch and TorchVision are pre-installed in Kaggle
# We don't need to reinstall them
